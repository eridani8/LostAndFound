<UserControl x:Class="LostAndFound.Views.Dialogs.CreateCategoryDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LostAndFound.Views"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1200"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <ScrollViewer Margin="20">
        <StackPanel>
            <ui:TextBox x:Name="CategoryName"
                        Margin="0,0,0,10"
                        PlaceholderText="Название категории" />
            <ui:TextBox x:Name="Description"
                        Margin="0,0,0,10"
                        PlaceholderText="Описание" />
        </StackPanel>
    </ScrollViewer>
</UserControl>